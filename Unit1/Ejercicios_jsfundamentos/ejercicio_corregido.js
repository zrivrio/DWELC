let suma = (a,b) => a+b; //Ya que es una simple suma es mas facil hacerlo de este tipo

let a = parseInt(prompt("Introduce el primer numero:")); //Se utiliza el paseint como en java o tambien si queremso que sea un numero podemos ponerle un + delante del valor
let b = parseInt(prompt("Introduce el segundo numero:"));
